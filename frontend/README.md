# JGG Frontend

Vite + React 기반 프론트엔드입니다. 기존 정적 페이지의 시각 요소를 React 컴포넌트로 분리했습니다.

## 실행

```bash
npm install
npm run dev
```

## 환경 변수

`.env.example`을 복사해 `.env`로 만들고 API 주소를 조정할 수 있습니다.

```text
VITE_API_BASE_URL=http://localhost:8000
```
